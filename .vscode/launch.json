{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run Discord Bot",
      "program": "${workspaceFolder}/src/index.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}